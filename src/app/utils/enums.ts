export enum PlanEnum {
  BASIC = 'basic',
  STANDARD = 'standard',
  PREMIUM = 'premium',
}

export enum PaymentStatus {
  PAID = 'paid',
}

export enum FormState {
  INITIAL = 'initial',
  SUCCESS = 'success',
  ERROR = 'error',
  LOADING = 'loading',
}